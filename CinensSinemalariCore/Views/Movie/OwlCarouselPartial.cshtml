@using EntityLayer.Concrete
@model List<Movie>


<div class="slider">
        <div class="owl-carousel owl-theme">
            @foreach (var item in Model)
            {
                <div class="item"> <img src="@item.MovieHorizontalImageUrl" alt=""> </div>
            }
        </div>
    </div>